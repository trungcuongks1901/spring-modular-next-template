# Architecture

This repository uses clean architecture by feature module with bounded contexts: masterdata, identity, business, shared.
ArchUnit rules enforce boundaries and forbid controller-to-repository coupling.
